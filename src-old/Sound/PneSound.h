#ifndef PNESOUND_H
#define PNESOUND_H

#include <Utils/PneUtilities.h>
#include <pne_config.h>

/*class PneSound
{
public:
    enum SndType
    {
        SndMusic = 0,
        SndFX2D = 1,
        SndFX3D = 2,
    } sndType;

    PneSound(std::string& filename, SndType type);

    void Play();
    void Pause();
    void Stop();

    //GET
    sf::SoundBuffer getSoundBuffer();
    //SET
    void setSoundBuffer(sf::SoundBuffer buffer);

private:
    sf::SoundBuffer sndBuffer;
    sf::Sound _sound;
    sf::Music _music;
};*/

#endif // PNESOUND_H
